DATABASE_URL=postgresql://apple:localdev@localhost:5432/clickup_clone

JWT_ACCESS_SECRET=change-me-access-secret
JWT_REFRESH_SECRET=change-me-refresh-secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

# ── Google OAuth ──
# Create at https://console.cloud.google.com → APIs & Services → Credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:4000/api/auth/google/callback

# ── Email ──
# EMAIL_PROVIDER: "console" (logs links to terminal) or "resend" (sends real email)
EMAIL_PROVIDER=console
# Get an API key at https://resend.com → API Keys
RESEND_API_KEY=
# Must be an address on a domain you've verified in Resend (e.g. "Flowspace <noreply@syspreedigital.com>")
EMAIL_FROM=

# ── Zoom (Server-to-Server OAuth app) ──
# Create at https://marketplace.zoom.us → Develop → Build App → Server-to-Server OAuth
# Grant scope: meeting:write:meeting:admin (and meeting:read:meeting:admin)
ZOOM_ACCOUNT_ID=
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=

STORAGE_PROVIDER=local
UPLOAD_DIR=./uploads

# ── File storage (Cloudflare R2) ──
# Only needed when STORAGE_PROVIDER=r2. Create a bucket + API token at
# https://dash.cloudflare.com → R2 → Manage R2 API Tokens (needs Object Read & Write).
# R2_ACCOUNT_ID is the account-level ID shown on the R2 overview page, not the bucket name.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=

CORS_ORIGIN=http://localhost:3000
API_PORT=4000

NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
